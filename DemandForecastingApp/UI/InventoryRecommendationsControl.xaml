<UserControl x:Class="DemandForecastingApp.UI.InventoryRecommendationsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <ListView Name="RecommendationsListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Item" DisplayMemberBinding="{Binding Item}" Width="150"/>
                    <GridViewColumn Header="Current Stock" DisplayMemberBinding="{Binding CurrentStock}" Width="100"/>
                    <GridViewColumn Header="Recommended Order" DisplayMemberBinding="{Binding RecommendedOrder}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
